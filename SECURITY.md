# Security Policy

This repository contains low-level Android recovery and device-specific integration.

Do not commit signing keys, GitHub tokens, SSH private keys, OTA private keys, credentials, or sensitive device data.

Security-sensitive findings should be reported privately to the maintainer.

Binary inputs and canonical patches are SHA-256 tracked through:

- `manifests/device-blobs.sha256`
- `patches/SHA256SUMS`
